# Production Environment Variables for Railway
# Copy these to Railway dashboard → Variables

# Server Configuration
PORT=8000
ENVIRONMENT=production

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# GitHub OAuth
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret
GITHUB_REDIRECT_URL=https://your-backend.up.railway.app/api/auth/callback
GITHUB_TOKEN=your_github_personal_access_token

# Frontend URL (Vercel)
FRONTEND_URL=https://your-app.vercel.app

# NVIDIA API (for AI features)
NVIDIA_API_KEY=your_nvidia_api_key

# IMPORTANT NOTES:
# 1. ENVIRONMENT=production enables secure cookies (Secure=true, SameSite=None)
# 2. FRONTEND_URL must match your Vercel deployment URL exactly (no trailing slash)
# 3. GITHUB_REDIRECT_URL must match the callback URL in your GitHub OAuth app
# 4. All URLs must use HTTPS in production
